{
  "name": "berlin-wahllm-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.18.0 <25"
  },
  "scripts": {
    "build": "node scripts/generate-third-party-notices.mjs && observable build && node scripts/finalize-build.mjs",
    "dev": "observable preview",
    "test": "node --test"
  },
  "dependencies": {
    "@observablehq/framework": "1.13.4",
    "@observablehq/inputs": "0.12.0",
    "@observablehq/plot": "0.6.17"
  }
}
